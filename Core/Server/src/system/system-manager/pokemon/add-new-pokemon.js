// import { addNewPokemon } from 'system/query-manager/pokemon'

export const addNewPokemonManager = () =>
  new Promise((resolve, reject) => {
    try {

    } catch (error) {
      console.log(error)
      reject(error)
    }
  })
