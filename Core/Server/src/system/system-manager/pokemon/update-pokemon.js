// import { updatePokemon } from 'system/query-manager/pokemon'

export const updatePokemonManager = () =>
  new Promise((resolve, reject) => {
    try {

    } catch (error) {
      console.log(error)
      reject(error)
    }
  })
