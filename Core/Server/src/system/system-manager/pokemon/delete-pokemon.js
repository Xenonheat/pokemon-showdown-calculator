// import { deletePokemon } from 'system/query-manager/pokemon'

export const deletePokemonManager = () =>
  new Promise((resolve, reject) => {
    try {

    } catch (error) {
      console.log(error)
      reject(error)
    }
  })
